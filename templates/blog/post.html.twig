{% extends 'base.html.twig' %}

{% block titlepage %}{{ article.title }}{% endblock %}

{% block imageHeader %}{{ article.picture }}{% endblock %}
{% block meta %}Posté par {{ article.user.firstname }} {{ article.user.lastname }} le {{ article.createdAt | date('d/m/Y') }}- Dans 
        {% for category in article.categories %}
            <a href="{{ path('category', {id:category.id}) }}">{{ category.title }}</a> 
        {% endfor %} 
{% endblock %}




{% block body %}
    <article>
        {{ article.content | raw}}
    </article>
    <hr>

{% endblock %}
